# Spring Data JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Datasource Configuration (PostgreSQL)
spring.datasource.url=jdbc:postgresql://dpg-d1iemkumcj7s738q6bhg-a.oregon-postgres.render.com/music_app_db_x6cj
spring.datasource.username=music_user
spring.datasource.password=HaX10fWlVc2ofm2XoPTUNifibALNr9pO
spring.datasource.driver-class-name=org.postgresql.Driver

# Render specific for production
# This assumes your Render environment variables are set up correctly.
# If you are using Render's built-in PostgreSQL, you might want to use:
# spring.datasource.url=${DATABASE_URL}
# Otherwise, stick to the hardcoded URL provided.